// Minimal root layout required by Next.js.
// The <html> and <body> tags, along with locale-specific attributes,
// are provided by app/[locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
